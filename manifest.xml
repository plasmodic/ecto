<package>
<description brief="ecto">ecto is ...</description>
<author>Ethan Rublee, Troy Straszheim</author>
<license>BSD</license>
<depend package="rospy" />
<export>
    <cpp cflags="-I${prefix}/include" lflags="-L${prefix}/build/lib -lecto"/>
    <python path="${prefix}/build/lib" />
</export>

<rosbuild2>
<export>
  <include_dir>${${PROJECT_NAME}_SOURCE_DIR}/include</include_dir>
  <lib>${PROJECT_NAME}</lib>
</export>
</rosbuild2>

</package>
