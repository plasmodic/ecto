include_directories(lib)
add_subdirectory(lib)
add_subdirectory(pybindings)

#add_executable(asio_sketch asio_sketch.cpp)

#target_link_libraries(asio_sketch 
#  ${Boost_THREAD_LIBRARY} ${Boost_SYSTEM_LIBRARY})
